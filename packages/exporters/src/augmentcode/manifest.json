{
  "name": "augmentcode",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to AugmentCode .augment/rules/ directory format",
  "outputs": [".augment/rules/*.md"],
  "handler": "./index.ts",
  "license": "MIT",
  "fidelityNotes": [
    "Scope-based file organization (one .md per scope)",
    "Machine-checkable rules (check) not represented",
    "Autofix hints not represented"
  ]
}


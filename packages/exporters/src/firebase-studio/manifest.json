{
  "name": "firebase-studio",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Firebase IDX .idx/*.md multi-file or GEMINI.md format",
  "outputs": [".idx/*.md"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "Machine-checkable rules (check) not represented in .idx/*.md format",
    "Autofix hints not represented in .idx/*.md format"
  ],
  "supportedFormats": ["native", "agents-md"],
  "defaultFormat": "native"
}

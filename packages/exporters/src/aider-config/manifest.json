{
  "name": "aider-config",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Aider .aider.conf.yml configuration format",
  "outputs": [".aider.conf.yml"],
  "handler": "./index.ts",
  "license": "MIT",
  "fidelityNotes": [
    "Machine-checkable rules (check) not represented in .aider.conf.yml format",
    "Autofix hints not represented in .aider.conf.yml format",
    "Complements AGENTS.md exporter (Aider uses both files)"
  ]
}


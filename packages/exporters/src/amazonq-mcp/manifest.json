{
  "name": "amazonq-mcp",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Amazon Q .amazonq/mcp.json MCP configuration format",
  "outputs": [".amazonq/mcp.json"],
  "handler": "./index.ts",
  "license": "MIT",
  "fidelityNotes": [
    "Machine-checkable rules (check) not represented in MCP config format",
    "Autofix hints not represented in MCP config format"
  ]
}


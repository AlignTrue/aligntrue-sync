{
  "name": "opencode",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to AGENTS.md format (opencode compatible)",
  "outputs": ["AGENTS.md"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "Uses universal AGENTS.md format",
    "opencode also uses opencode.json (separate exporter)"
  ]
}

{
  "name": "gemini-cli",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to AGENTS.md format (Gemini CLI compatible)",
  "outputs": ["AGENTS.md"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "Uses universal AGENTS.md format",
    "Gemini CLI also uses .gemini/settings.json (separate exporter)"
  ]
}

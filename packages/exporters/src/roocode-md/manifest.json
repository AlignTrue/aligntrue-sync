{
  "name": "roocode-md",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to AGENTS.md format (RooCode compatible)",
  "outputs": ["AGENTS.md"],
  "handler": "../agents-md/index.ts",
  "license": "MIT",
  "fidelityNotes": [
    "Uses universal AGENTS.md format",
    "RooCode also uses .roo/mcp.json (separate exporter)"
  ]
}

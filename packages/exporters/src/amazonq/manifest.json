{
  "name": "amazonq",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Amazon Q .amazonq/rules/ directory format",
  "outputs": [".amazonq/rules/*.md"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "One markdown file per rule, preserving original filenames",
    "Machine-checkable rules (check) not represented",
    "Autofix hints not represented",
    "applies_to patterns preserved but not enforced"
  ]
}

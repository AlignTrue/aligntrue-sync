{
  "name": "firebase-mcp",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Firebase Studio .idx/mcp.json MCP configuration format",
  "outputs": [".idx/mcp.json"],
  "handler": "./index.ts",
  "license": "MIT",
  "fidelityNotes": [
    "Machine-checkable rules (check) not represented in MCP config format",
    "Autofix hints not represented in MCP config format"
  ]
}


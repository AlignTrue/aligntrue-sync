{
  "name": "cursor-mcp",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Cursor .cursor/mcp.json MCP configuration format",
  "outputs": [".cursor/mcp.json"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "Extracts vendor.cursor fields to top level",
    "Machine-checkable rules (check) not represented in MCP config format",
    "Autofix hints not represented in MCP config format"
  ]
}

{
  "name": "trae-ai",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Trae AI .trae/rules/ directory format",
  "outputs": [".trae/rules/*.md"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "One markdown file per rule, preserving original filenames",
    "Machine-checkable rules (check) not represented",
    "Autofix hints not represented"
  ]
}

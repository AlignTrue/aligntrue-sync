{
  "name": "goose",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Goose .goosehints format",
  "outputs": [".goosehints"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "Machine-checkable rules (check) not represented in .goosehints format",
    "Autofix hints not represented in .goosehints format"
  ]
}

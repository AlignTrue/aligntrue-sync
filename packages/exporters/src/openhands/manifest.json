{
  "name": "openhands",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to OpenHands .openhands/microagents/ format (one file per rule)",
  "outputs": [".openhands/microagents/*.md"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "Machine-checkable rules (check) not represented",
    "Autofix hints not represented"
  ]
}

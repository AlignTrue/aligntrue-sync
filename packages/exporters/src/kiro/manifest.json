{
  "name": "kiro",
  "version": "1.0.0",
  "description": "Export AlignTrue rules to Kiro .kiro/steering/ directory format",
  "outputs": [".kiro/steering/*.md"],
  "handler": "./index.js",
  "license": "MIT",
  "fidelityNotes": [
    "One markdown file per rule, preserving original filenames",
    "Machine-checkable rules (check) not represented",
    "Autofix hints not represented"
  ]
}

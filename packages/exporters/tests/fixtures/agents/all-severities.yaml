id: test-align
version: 1.0.0
spec_version: "1"
sections:
  - heading: Security No Secrets
    level: 2
    content: |
      Never commit secrets or API keys to version control.
    fingerprint: security-no-secrets
  - heading: Testing Requirements
    level: 2
    content: |
      All features should have tests.
    fingerprint: testing-require-tests
  - heading: Style Prefer Const
    level: 2
    content: |
      Use const instead of let when variables are not reassigned.
    fingerprint: style-prefer-const

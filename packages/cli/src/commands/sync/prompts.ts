/**
 * Interactive prompts for sync command
 *
 * Note: In centralized mode (default), new files are handled via
 * detectAndHandleUntrackedFiles and detectAndEnableAgents in context-builder.ts
 * which extract old content and enable as export targets only.
 */

// This file is kept for future use and backward compatibility
// All sync prompts are now handled inline in context-builder.ts

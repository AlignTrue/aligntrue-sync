# Sync package consolidation (placeholder)

Reserved for potential future consolidation of sync CLI packages.

- No code moves yet.
- Current layout remains under `packages/cli`, `packages/core`, `packages/schema`, `packages/exporters`, `packages/sources`, and `packages/file-utils`.
- See `.aligntrue/rules/platform_structure.md` for the current map.

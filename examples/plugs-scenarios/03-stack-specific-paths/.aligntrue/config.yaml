version: "1"
mode: solo

sources:
  - type: local
    path: "base-pack.yaml"
  - type: local
    path: "nextjs-pack.yaml"

plugs:
  fills:
    config.file: "next.config.js"
    env.file: ".env.local"
    build.output: ".next"
    tsconfig.file: "tsconfig.json"
    package.manager: "pnpm"

exporters:
  - agents-md
  - cursor

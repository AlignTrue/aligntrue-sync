# Generated state files (local state, not part of source control)
.source-rule-hashes.json
.agent-export-hashes.json
.rules.yaml

# Backup directory (created during sync operations)
.backups/


























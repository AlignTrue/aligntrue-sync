---
title: "Quickstart"
description: "Get AlignTrue running in 60 seconds. Auto-detect agents and sync your first rules."
---

# Quickstart guide

Get AlignTrue running in under 60 seconds with three commands.

## Prerequisites

- **Node.js 20+** - [Download](https://nodejs.org/)
- **An AI coding agent** - Cursor, GitHub Copilot, Claude Code, or any of [28+ supported agents](/docs/04-reference/agent-support)

## Install AlignTrue

```bash
npm install -g aligntrue
```

<details>
<summary>Prefer a different package manager?</summary>

<Tabs items={["yarn", "pnpm", "bun"]}>

<Tabs.Tab>```bash yarn global add aligntrue ```</Tabs.Tab>

<Tabs.Tab>```bash pnpm add -g aligntrue ```</Tabs.Tab>

<Tabs.Tab>```bash bun install -g aligntrue ```</Tabs.Tab>

</Tabs>

</details>

**Alternative:** Use npx for one-off commands without installing: `npx aligntrue init`

## Quick start (3 commands)

### 1. Initialize

```bash
cd your-project
aligntrue init
```

This auto-detects any existing agent files (Cursor `.mdc`, `CLAUDE.md`, etc.), imports them into AlignTrue, and only creates a fresh `AGENTS.md` starter when no agent files exist. After the import it writes:

- `.aligntrue/config.yaml` - Configuration with detected exporters
- `.aligntrue/rules` - Internal format (auto-generated, don't edit)
- `AGENTS.md` - **Only** when you explicitly create a starter file during init

> **Tip:** At the end of `aligntrue init`, you'll be prompted to run `aligntrue sync`. Confirm to export immediately or decline and run it later when you're ready.

### 2. Sync to your agents

```bash
aligntrue sync
```

This syncs to all your detected agents in their native formats:

- `.cursor/rules/*.mdc` (if Cursor detected - one file per rule)
- `AGENTS.md` (universal format for Copilot, Claude, Aider, etc.)
- `.vscode/mcp.json` (if VS Code MCP detected)

### 3. Customize your rules

Edit rules in `.aligntrue/rules/`. Create `.md` files for your rule categories:

```markdown
# Project rules

## Use TypeScript strict mode

Enable strict mode in tsconfig.json for better type safety. This catches more errors at compile time and makes refactoring safer.

Set these in tsconfig.json:

- noImplicitAny: true
- strictNullChecks: true
- strictFunctionTypes: true
```

**That's it!** Save to `.aligntrue/rules/typescript.md`, then run `aligntrue sync` to export to all agents.

{/* prettier-ignore-start */}

<Callout type="info">
**Where to edit:** Only in `.aligntrue/rules/` directory

Edit markdown files in `.aligntrue/rules/`. All agent files (Cursor `.mdc`, `AGENTS.md`, etc.) are read-only exports.

Data flow:

```
Edit files in .aligntrue/rules/
    ↓
aligntrue sync (loads rules, validates, exports)
    ↓
All agent files (read-only, auto-generated)
```

**Important:** Agent files are under AlignTrue's control. Edits to agent files will be backed up and overwritten on the next sync. Always edit `.aligntrue/rules/` instead.

**Want continuous auto-sync?** Run `aligntrue watch` to automatically sync on file changes. See [File Watcher Setup](/docs/04-reference/file-watcher-setup).

</Callout>
{/* prettier-ignore-end */}

## What you get

After running `aligntrue sync`, you'll have agent-specific rule files:

**For Cursor:**

- `.cursor/rules/*.mdc` - Rules in Cursor's native format (one file per rule)

**For GitHub Copilot, Claude Code, Aider, and others:**

- `AGENTS.md` - Universal markdown format

**For VS Code with MCP:**

- `.vscode/mcp.json` - Model Context Protocol configuration (if enabled)

## Verify it works

Let's confirm your agent is using the rules:

### Cursor verification

1. Open Cursor Settings (Cmd/Ctrl + ,)
2. Navigate to Features > Cursor Rules
3. Enable "Rules folder" option (if not already enabled)
4. Restart Cursor
5. Open a file and start a chat
6. Ask: "What are the TypeScript rules for this project?"
7. Cursor should reference your rules from `AGENTS.md`

**Expected:** Cursor responds with your project's rules

**Problem?** See the [troubleshooting guide](/docs/05-troubleshooting).

### GitHub Copilot / Claude Code verification

These agents automatically read `AGENTS.md` from your project root - no setup needed!

1. Open a file
2. Start typing code that might violate a rule
3. Watch for suggestions that follow your rules

**Problem?** Make sure `AGENTS.md` exists in your project root:

```bash
ls -la AGENTS.md  # Should show the file
```

### Common issues

**Rules not showing in my agent:**

1. Verify files were created: `ls .cursor/rules/` or `ls AGENTS.md`
2. Restart your IDE completely
3. Check agent settings to ensure rules are enabled
4. Run `aligntrue sync` again to regenerate files

**Still not working?** See the [troubleshooting guide](/docs/05-troubleshooting).

Need deeper checks (VS Code MCP, Windsurf, Warp, etc.)? See the [agent verification guide](/docs/04-reference/agent-verification).

## How it works

AlignTrue uses a simple three-step process:

1. **Write rules** - Edit `AGENTS.md` in natural markdown
2. **Generate IR** - AlignTrue converts to internal format (`.aligntrue/rules`)
3. **Export** - Rules sync to all configured agents (`.cursor/*.mdc`, `AGENTS.md`, etc.)

**Workflow:**

```
AGENTS.md (you edit) → .aligntrue/rules (auto-generated) → Agent exports
```

**Key concepts:**

- **Rules** - Guidelines for AI agents (e.g., "use TypeScript strict mode")
- **Sync** - Convert rules into each agent's native format
- **Agents** - AI coding assistants (Cursor, Copilot, Claude Code, etc.)

See the [Glossary](/docs/03-concepts/glossary) for complete terminology.

## Next steps

**Learn more:**

- [Next Steps](/docs/00-getting-started/01-next-steps) - Auto-sync, team mode, git workflows
- [Natural Markdown Sections](/docs/04-reference/natural-markdown-sections) - Authoring reference and examples
- [Solo vs. Team Mode](/docs/00-getting-started/02-solo-vs-team-mode) - Choosing your mode

**Examples:**

- [Golden Repository](https://github.com/AlignTrue/aligntrue/tree/main/examples/golden-repo) - Complete working demo
- [Example Aligns](/docs/04-reference/examples) - 11 curated rule aligns

## See also

- Understand [agent support](/docs/04-reference/agent-support)
- Author rules in [natural markdown sections](/docs/04-reference/natural-markdown-sections)
- Learn about [sync behavior](/docs/03-concepts/sync-behavior) and how rules flow from source to exports
- Configure [.aligntrue/config.yaml](/docs/04-reference/config-reference)

## Enable team mode (optional)

For teams that need governance and approval workflows:

```bash
aligntrue team enable
# Prompts for lockfile mode (soft/strict)
# Shows configuration summary
```

This enables:

- Lockfile validation
- Bundle approval workflow
- Optional team-managed sections
- CI drift detection

See [Team mode guide](/docs/01-guides/04-team-guide) for details.

**Need help?**

- [FAQ](/docs/00-getting-started/03-faq) - Common questions
- [Troubleshooting](/docs/05-troubleshooting) - Common issues

---

**That's it!** You now have consistent AI rules across all your coding agents.

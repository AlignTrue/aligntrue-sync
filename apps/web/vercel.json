{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "public": false,
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": true
  },
  "rewrites": [
    {
      "source": "/docs/:path*",
      "destination": "https://aligntrue-docs.vercel.app/docs/:path*",
      "_comment": "Production-only: Proxies /docs requests to external Nextra site. In development, this returns 404 (expected behavior - rewrites don't resolve external URLs locally)."
    },
    {
      "source": "/sitemap.docs.xml",
      "destination": "https://aligntrue-docs.vercel.app/sitemap.docs.xml",
      "_comment": "Production-only: Fetches docs sitemap from external site for SEO consolidation."
    }
  ]
}
